name: Train Retention Model CI/CD

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  train:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run training pipeline
        run: |
          python run_training.py

      - name: Upload model artifact
        uses: actions/upload-artifact@v4
        with:
          name: trained-retention-model
          path: models/retention_model.joblib

      - name: Publish trained model to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          tag_name: retention-model-${{ github.run_number }}
          name: Retention Model ${{ github.run_number }}
          files: models/retention_model.joblib
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
